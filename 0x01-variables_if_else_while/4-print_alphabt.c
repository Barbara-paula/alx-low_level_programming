#include<stdio.h>
/**
 * main - entry point
 * prints in lowercase followed by a new line
 * Return: 0
 */
int main(void)
{
	int a = 97;

	while (a <= 122)
	{
		if (a == 101 || a == 113)
		{
			a++;
			continue;
		}
		putchar(a);
			n++;
	}
	putchar('\n');
	return (0);
}
	
